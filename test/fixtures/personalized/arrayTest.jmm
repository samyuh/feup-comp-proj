import io;
class T  {
    public static void main(String[] s) {
        int[] a;
        int c;
        T t;

        t = new T();
        a = new int[0];
        c = a[4];        // First test -> Array access must be an integer
        d = new int[6];  // Array initialization must be an integer
        c = t.retornaIntArray()[10];          // Can't attempt array access on a different data structure

        // Third Test -> Can't sum arrays directly
        /*
        int total;
        int[] arr1 = new int[10];
        int[] arr2 = new int[10];

        total = arr1 + arr2;
        */
    }

    public int retornaInt(){
        return 1;
    }

    public int[] retornaIntArray(){
        int[] a;
        a = new int[1];
        return a;
    }
}