/*  REASON:
    Array indices must be integer.
    Var "v" is undefined.
    Var "undeclaredVar" is undefined.
*/
import ol.aaa.casd;
import dedede.xcsdcd;
class T extends treta {
    int[] i;
    int maximum;
    int value;
    public static void main(String[] s) {
        int[] a;
        int c;
        a = b - c + d;      // b,d undefined
        a = (nao - eu + existe) * 6 + 2 / piu;  // nao, eu, existe undefined
        // This is valid in Java
        a = new int[0];
        c = truru[2]; // truru undefined
        c = a.getLen(d); // d undefined

    }

    public boolean asneira(){
        boolean result;

        if (maximum < 0){
            result = false;
        } else {
            result = true;
        }
        return result;
    }

    public int methodFoo(Boolean p1, int[] p2, String p3){
        int v0;
        int[] v1;
        Boolean v2;
        String v3;
        v = v3 + undeclaredVar - 7 - m * (5 + kkk); // v,undelcaredVar,m,kkk undefined
        a = (b + 6 + c) * 5;

        return (returnVal + 5) * lul; // returnVal, lul undefined
    }

    /*
        public Report ola(){
            Report r = new Report();
            return r;
        }
    */
}
