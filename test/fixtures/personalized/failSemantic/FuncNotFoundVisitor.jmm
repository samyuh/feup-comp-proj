/*  REASON:
    Variable assignments to literals must have the literal be the variable's type.
    Status: OK.
*/

import acceptImport;
class T {
    public static void main(String[] a) {
        int ola;
        Report report;

        ola = a + b + u;
        report.getName(y);
        io.println(10);
        a.getName();
        acceptImport.println(10);
    }

    public int foo(boolean a){
        int n;
        this.bar();

        if (this.ifBarError()){
            n = 0;
        } else {
            n = 0;
        }

        if (this.ifBarAccept()){
            n = 0;
        } else {
            n = 0;
        }

        while(this.whileBarError()){
            n = 0;
        }

        while(this.whileBarAccept()){
            n = 0;
        }

        return n;
    }

    public int ifBarAccept(){
        return 1;
    }

    public int whileBarAccept(){
        return 1;
    }
}
