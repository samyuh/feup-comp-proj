import io;
class Fac {
    public int compFac(int num){
        int num_aux;
        if (num < 1){
            num_aux = 1;
        }
        else
            num_aux = num * (this.compFac(num));
        return num_aux;
    }
}