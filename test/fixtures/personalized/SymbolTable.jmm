/*  REASON:
    Array indices must be integer.
    Var "v" is undefined.
    Var "undeclaredVar" is undefined.
*/
import ol.aaa.casd;
import dedede.xcsdcd;
class T extends treta {
    int[] i;
    int maximum;
    int value;
    public static void main(String[] s) {
        int[] a;
        int c;
        a = b - c + d;
        // This is valid in Java
        a = new int[0];
        c = truru[2];
        c = a.getLen(d);

    }

    public boolean asneira(){
        boolean result;

        if (maximum < 0){
            result = false;
        } else {
            result = true;
        }
        return result;
    }

    public int methodFoo(Boolean p1, int[] p2, String p3){
        int v0;
        int[] v1;
        Boolean v2;
        String v3;
        v = v3 + undeclaredVar - 7 - m * (5 + kkk);

        return j;
    }

    /*
        public Report ola(){
            Report r = new Report();
            return r;
        }
    */
}
